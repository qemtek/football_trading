version: 2
jobs:
  test_model:
    working_directory: ~/project
    docker:
      - image: circleci/python:3.7.2
    steps:
      - checkout
      - run:
          name: Running Tests
          command: |
            echo $PWD
            virtualenv venv
            . venv/bin/activate
            pip install --upgrade pip
            pip install -r packages/football_trading/requirements.txt
            export PROJECTSPATH=$CCI_PROJECTSPATH >> $BASH_ENV
            export IN_PRODUCTION=false >> $BASH_ENV
            export DB_DIR=$CCI_DB_DIR >> $BASH_ENV
            export BFEX_USER=$CCI_BFEX_USER >> $BASH_ENV
            export BFEX_PASSWORD=$CCI_BFEX_PASS >> $BASH_ENV
            export BFEX_APP_KEY=$CCI_BFEX_APP_KEY >> $BASH_ENV
            export BFEX_CERTS_PATH=$CCI_BFEX_CERTS_PATH >> $BASH_ENV
            export PRODUCTION_MODEL_NAME=match-predict-base >> $BASH_ENV
            export RECREATE_DB=false >> $BASH_ENV
            # Should we download any data here?
            py.test -vv packages/football_trading/tests
  train_and_upload_model:
    working_directory: ~/project
    docker:
      - image: circleci/python:3.7.2
    steps:
      - checkout
      - run:
          name: Setup env
          command: |
            echo $PWD
            virtualenv venv
            . venv/bin/activate
            pip install --upgrade pip
            pip install -r packages/football_trading/requirements.txt
      - run:
          name: Publish model
          command: |
            echo $PWD
            echo $CCI_PROJECTSPATH
            . venv/bin/activate
            export PROJECTSPATH=$CCI_PROJECTSPATH >> $BASH_ENV
            export IN_PRODUCTION=false >> $BASH_ENV
            export DB_DIR=$CCI_DB_DIR >> $BASH_ENV
            export BFEX_USER=$CCI_BFEX_USER >> $BASH_ENV
            export BFEX_PASSWORD=$CCI_BFEX_PASS >> $BASH_ENV
            export BFEX_APP_KEY=$CCI_BFEX_APP_KEY >> $BASH_ENV
            export BFEX_CERTS_PATH=$CCI_BFEX_CERTS_PATH >> $BASH_ENV
            export PRODUCTION_MODEL_NAME=match-predict-base >> $BASH_ENV
            export RECREATE_DB=false >> $BASH_ENV
            chmod +x ./scripts/publish_model.sh
            export PYTHONPATH=./packages/football_trading
            python ./packages/football_trading/football_trading/train.py
            ./scripts/publish_model.sh ./packages/football_trading/
workflows:
  version: 2
  test-all:
    jobs:
      - test_model
      - train_and_upload_model
